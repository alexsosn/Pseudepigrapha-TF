name: issue82-apply

on:
  push:
    branches:
      - fix/public-metadata-feature-ownership

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/public-metadata-feature-ownership
      - name: Validate staging runner
        run: echo issue82-staging-ready

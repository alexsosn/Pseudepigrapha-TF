# Issue #158 plan

1. **RED test.** Extend `tests/test_blank_source_identifiers.py` with one integration test using the sanctioned direct-model fixture. Add real named witness `A` and two anonymous, distinct, metadata-only manuscripts. Convert, serialize, load `Apparatus.WORK_PASSAGE_FEATURES` through raw Fabric. Assert all three TF manuscript nodes and names survive, anonymous nodes are not targets of `witness`, `Apparatus.passage()` returns only addressable `A`, and `work_passage()` likewise succeeds with `A`. On current main `_witnesses()` raises `duplicate manuscript abbreviation: '   '`.
2. **GREEN implementation.** In `_witnesses()` filter only blank/whitespace-only `ms_abbrev` *before building the keyed inventory*. Keep duplicate checks on nonblank sigla, preserve original nonblank sigla exactly, retain manuscript TF nodes, don't alter parser/converter or fabricate identifiers.
3. **Docs.** Add one sentence to `docs/apparatus.md` clarifying derived keyed inventory excludes unaddressable manuscripts and points to raw TF for their metadata.
4. **Verification.** Confirm clean RED CI against test-only commit; then implement, run focused/full 602+ unit tests and full pinned upstream integration on exact final head.
5. **Independent adversarial review.** Re-read the issue and final diff from scratch; challenge loss of data, blank versus nonblank distinction, synthetic witness exclusion, real declared witness attestation, both high-level entry points, unintended changes to other methods. Publish review directly on PR. Merge only after exact-head GREEN and review.

# Issue #158 plan

1. **Research.** Confirm converter accepts distinct unaddressable manuscript metadata, modern XML parser still forbids blank required attributes, and Text-Fabric reload represents the blank abbreviation as `None` while preserving manuscript nodes and names.
2. **RED test.** Add `tests/test_anonymous_manuscript_inventory.py` using the accepted direct-model fixture: three addressable manuscripts plus two distinct anonymous metadata-only records. Convert, serialize, load `Apparatus.WORK_PASSAGE_FEATURES` via raw Fabric, confirm five nodes and both anonymous names survive without witness edges. Both `passage()` and `work_passage()` should provide only `A`/`B`/`C` in their `witnesses` mapping. First test-only commit `980f7e1` erroneously called `.strip()` on a TF `None` value; corrected test-only RED `b50cfa0` reached production `_witnesses()` and CI 35119859983 recorded two `ValueError: feature 'ms_abbrev' has no value for node 7` failures with 602 existing passes.
3. **GREEN implementation.** In `_witnesses()`, skip only `None`/blank/whitespace-only `ms_abbrev` values before sorting and required-value access. Preserve duplicate detection on nonblank sigla, underlying metadata nodes, synthetic witness exclusion and fail-closed feature-loading behavior. Do not modify parser/converter or fabricate identifiers.
4. **Documentation.** Specify in `docs/apparatus.md` that only addressable manuscript sigla appear in keyed inventories; anonymous metadata remains in raw TF.
5. **Verification and review.** Full unit/TF and pinned-upstream integration CI on exact clean PR head, followed by logically independent adversarial review posted on the PR. Review must challenge data preservation, accidental hiding of real witnesses, synthetic exclusion, both entry points, missing load-feature errors and unrelated API changes. Merge only after both green checks and review.
